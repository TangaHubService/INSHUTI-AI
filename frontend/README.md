# Inshuti — frontend

Next.js 14 (App Router, TypeScript) UI. See the [repo root README](../README.md)
for full setup instructions — this app is meant to run alongside `../backend`.

```bash
npm install
npm run dev
```
